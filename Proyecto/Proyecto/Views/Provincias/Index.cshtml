@model IEnumerable<Proyecto.Models.Provincia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IdProvincia">Edit</a> |
                <a asp-action="Details" class="btn btn-info" asp-route-id="@item.IdProvincia">Details</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdProvincia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
